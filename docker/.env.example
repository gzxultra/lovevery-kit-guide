# =============================================================================
# Lovevery Alternatives Scraper — Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# ── Required ────────────────────────────────────────────────────────────────

# OpenAI API Key — used for AI-powered product search
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# GitHub Personal Access Token — used to push updated data
# Create one at: https://github.com/settings/tokens
# Required scopes: repo (Full control of private repositories)
GITHUB_TOKEN=ghp_your-github-token-here

# ── Optional (defaults shown) ──────────────────────────────────────────────

# GitHub repository in owner/repo format
# GITHUB_REPO=gzxultra/loveveryfans

# Cron schedule expression (default: every Monday at 3 AM)
# CRON_SCHEDULE=0 3 * * 1

# Whether to run the scraper immediately when the container starts
# RUN_ON_STARTUP=true

# Timezone for cron scheduling
# TZ=America/Los_Angeles

# Scraper flags
# SCRAPER_FLAGS=--update --verbose
